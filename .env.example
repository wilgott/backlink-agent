# Gmail OAuth client secret JSON (downloaded from Google Cloud Console).
# Path to the client-secret file used by `python -m backlink_agent auth gmail`.
BACKLINK_AGENT_GMAIL_CLIENT_SECRET=

# Where the authorized Gmail token is cached after `auth gmail` completes.
BACKLINK_AGENT_GMAIL_TOKEN_PATH=~/.config/backlink-agent/gmail_token.json
